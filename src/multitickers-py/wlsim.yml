calendar: scripts/ChinaTradingDates.txt
start: 20230101
end: 20230109

secmaster:
  config:
    # input_dir: /global/wlsim/data/secmaster

marketdata:
  - type: snapshot
    module: snapshot
    config:
    # input_dir: /global/wlsim/data/cffex_snapshot

signal:
  module: py
  config:
    name: pymultiticker
    module: nickchenyj-multitickers_py
    pylib: libpython3.9.so
    pypath:
      - build/Debug/src/multitickers-py/
    config_file: src/multitickers-py/sig.yml
    output:
      module: csv
      config:
        output_dir: output

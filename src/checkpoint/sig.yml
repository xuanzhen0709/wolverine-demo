targets:
  - dynamic:stocks

marketdata:
  - module: cs-snapshot
    symbols:
      - stocks
    options:
      # data_dir: /global/wlsim/data/cs_snapshot
      fields:
        - ap
        - last_price
        - volume

# freq can be 60/120/180
freq: 120

targets:
  - dynamic:stocks

marketdata:
  - module: cs-snapshot
    symbols:
      - stocks
    options:
      fields:
        - last_price
        - volume
        - ap
      levels: 1

  - module: cs-mbo
    symbols:
      - stocks
# user defined params can go below

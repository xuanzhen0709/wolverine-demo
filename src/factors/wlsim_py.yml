calendar: /mnt/nas-3/CTA/Data/ChinaTradingDates.txt
start: 20230703
end: 20230710

refdata:
  # data_dir: /global/wlsim/data/refdata

worker:
  type: pool
  threads: 5

signal:
  name: strat
  module: py
  output:
    module: csv
    config:
      output_dir: output
  factors:
   - name: snapshot
     module: nickchenyj.factor
     config:
       seed: 1.234
   - name: snapshot
     module: nickchenyj.factor
     config:
       seed: 2.345
   - name: snapshot
     module: nickchenyj.factor
     config:
       seed: 3.456
   - name: snapshot
     module: nickchenyj.factor
     config:
       seed: 4.567
   - name: snapshot
     module: nickchenyj.factor
     config:
       seed: 5.678
   - name: snapshot
     module: nickchenyj.factor
     config:
       seed: 6.789
   - name: snapshot
     module: nickchenyj.factor
     config:
       seed: 7.890
   - name: snapshot
     module: nickchenyj.factor
     config:
       seed: 8.901
   - name: snapshot
     module: nickchenyj.factor
     config:
       seed: 9.012

  config:
    module: nickchenyj.stratpy
    pylib: libpython3.11.so
    config:
      targets:
        - dynamic:stocks
      marketdata:
        - module: cs-snapshot
          symbols:
            - stocks
          config:
            # data_dir: /data/cs_snapshot
            fields:
              - last_price
              - volume
              - ap
            levels: 5
        # - module: cs-mbo
        #   symbols:
        #     - stocks
        #   config:
        #     data_dir: /data/cs_mbo

env: {}
start: 20230505
end: 20230505

refdata:

marketdata:
  - module: snapshot
    symbols:
      - IC.CFFEX
      - IF1.CFFEX
      - IF2.CFFEX
    config: {}
    client:
      - multitickers

signal:
  name: multitickers
  module: nickchenyj.multitickers
  output:
    module: csv
    config:
      output_dir: output
  config:
    targets:
      - IC.CFFEX
      - IF1.CFFEX
    # user defined params can go below

env:

start: 20230701
end: 20230710

refdata:
  # data_dir: /global/wlsim/data/refdata

signal:
  name: snapshot
  module: nickchenyj.snapshot
  output:
    module: csv
    config:
      output_dir: output
  config:
    targets:
      - IC.CFFEX

    marketdata:
      - module: snapshot
        symbols:
          - IC.CFFEX
        config: {}

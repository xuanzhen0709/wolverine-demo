targets:
  - dynamic:stocks

marketdata:
  - module: cs-snapshot
    symbols:
      - stocks
    options:
      # data_dir: /data/
      fields:
        - last_price
        - volume
        - ap
      levels: 5

  - module: cs-mbo
    symbols:
      - stocks

freq: 120

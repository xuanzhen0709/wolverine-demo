env: {}
start: 20230513
end: 20230613

refdata:
  # data_dir: /global/wlsim/data/refdata

signal:
  name: csmbo
  module: nickchenyj.csmbo
  output:
    module: csv
    config:
      output_dir: output
  config:
    targets:
      - dynamic:stocks
    marketdata:
      - module: cs-snapshot
        symbols:
          - stocks
        config:
          data_dir: /mnt/nas-3/ProcessedData/tick_sliced_3s_bin/
          fields:
            - last_price
            - volume
            - ap
          levels: 5
      - module: cs-mbo
        symbols:
          - stocks
        config:
          data_dir: /mnt/nas-3/ProcessedData/tick_sliced_3s_bin/
    freq: 120

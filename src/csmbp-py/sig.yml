targets:
  - dynamic:stocks

marketdata:
  - module: cs-snapshot
    symbols:
      - stocks
    options:
      fields:
        - last_price
        - volume

  - module: cs-mbp
    symbols:
      - stocks
# user defined params can go below

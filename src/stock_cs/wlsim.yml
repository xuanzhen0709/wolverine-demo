calendar: data/ChinaTradingDates.txt
start: 20230101
end: 20230109

secmaster:
  config:
    input_dir: data/insinfo

marketdata:
  - type: section
    module: section
    config: {}

signal:
  module: cpp
  config:
    name: stocks_cs
    module: nickchenyj-stock_cs
    config_file: src/stock_cs/sig.yml
    output:
      module: csv
      config:
        output_dir: output

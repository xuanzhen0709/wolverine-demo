targets:
  - dynamic:stocks

marketdata:
  - module: cs-snapshot
    symbols:
      - stocks
    options:
      fields:
        - ap1
        - last_price
        - volume
      # data_dir: /data/

  - module: cs-mbp
    symbols:
      - stocks

# freq can be 60/120/180
freq: 120

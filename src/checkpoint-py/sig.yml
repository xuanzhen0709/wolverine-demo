targets:
  - dynamic:stocks

marketdata:
  - module: cs-snapshot
    symbols:
      - stocks
    options:
      # data_dir: /global/wlsim/data/cs_snapshot
      fields:
        - last_price
        - volume
# user defined params can go below

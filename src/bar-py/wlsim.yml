calendar: /mnt/nas-3/CTA/Data/ChinaTradingDates.txt
start: 20230101
end: 20230109

refdata:
  # data_dir: /global/wlsim/data/refdata

signal:
  name: bar-py
  module: py
  output:
    module: csv
    config:
      output_dir: output
  config:
    module: nickchenyj.bar
    pylib: libpython3.8.so
    config:
      targets:
        - IC.CFFEX
      marketdata:
        - module: bar
          symbols:
            - IC.CFFEX
          config:
            # data_dir: /global/wlsim/data/snapshot_bin
            freq: 5
      signal_param1: 20

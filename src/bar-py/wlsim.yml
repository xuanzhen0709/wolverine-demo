env:
  python_runtime: libpython3.8.so

start: 20230101
end: 20230109
refdata:
  # data_dir: /mnt/what

signal:
  name: bar-py
  module: py
  output:
    module: csv
    config:
      output_dir: /mnt/nas-3/homes/qsun/au.SHFE
  config:
    module: nickchenyj.bar
    config:
      targets:
        - IC.CFFEX
      marketdata:
        - module: bar
          symbols:
            - IC.CFFEX
          config:
            freq: 5

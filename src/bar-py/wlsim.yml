calendar: scripts/ChinaTradingDates.txt
start: 20230101
end: 20230109

refdata:
  config:
    # data_dir: /global/wlsim/data/refdata

signal:
  name: bar-py
  module: py
  config:
    module: nickchenyj.bar
    pylib: libpython3.8.so
    pypath:
      - build/Debug/src/bar-py/
    config_file: src/bar-py/sig.yml
  output:
    module: csv
    config:
      output_dir: output

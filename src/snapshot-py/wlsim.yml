calendar: scripts/ChinaTradingDates.txt
start: 20230101
end: 20230109

secmaster:
  config:
    # input_dir: /global/wlsim/data/secmaster

marketdata:
  - type: snapshot
    module: snapshot
    config:
    # input_dir: /global/wlsim/data/snapshot_bin

signal:
  module: py
  config:
    name: snapshot-py
    module: nickchenyj.snapshot
    pylib: libpython3.9.so
    pypath:
      - build/Debug/src/snapshot-py/
    config_file: src/snapshot-py/sig.yml
    output:
      module: npy
      config:
        output_dir: output

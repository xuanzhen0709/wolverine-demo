env:
  python_runtime: libpython3.9.so
start: 20230407
end: 20230407

refdata:
  # data_dir: /global/wlsim/data/refdata
  # ticker_mapping: CTA
  # ticker_mapping: HFT

signal:
  name: snapshot-py
  module: py
  output:
    module: npy
    config:
      output_dir: output
  config:
    module: nickchenyj.snapshot
    config:
      targets:
        - a.DCE

      marketdata:
        - module: snapshot
          symbols:
            - a.DCE
          config: {}

      # user defined params can go below

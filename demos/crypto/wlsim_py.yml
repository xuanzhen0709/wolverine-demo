env: {}

start: 20231208
end: 20231208

calendar: /mnt/nas-3/cta/Data/CryptoTradingDates.txt
refdata:
  ticker_mapping: CRYPTO

marketdata:
  - module: snapshot
    symbols:
      - ETHUSDT.BIANUM
    config:
      dataset: binance
    client:
      - snapshot

signal:
  name: snapshot
  module: nickchenyj.snapshot
  is_python: true
  output:
    module: csv
    config:
      output_dir: output
  config:
    targets:
      - ETHUSDT.BIANUM

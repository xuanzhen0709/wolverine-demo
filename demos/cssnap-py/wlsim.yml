env: {}
start: 20230101
end: 20230104
refdata: {}

marketdata:
  - module: cs-snapshot
    symbols:
      - stocks.CHN
    config:
      # data_dir: /global/wlsim/data/cs_snapshot
      fields:
        - last_price
        - volume
        - ap
      levels: 1
    client:
      - cssnap-py

signal:
  name: cssnap-py
  module: nickchenyj.cssnap
  is_python: true
  output:
    module: csv
    config:
      output_dir: output
  config:
    targets:
      - dynamic:stocks.CHN
    # user defined params can go below

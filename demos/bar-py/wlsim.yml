env: {}

start: 20230101
end: 20230109
refdata:
  # data_dir: /mnt/what

marketdata:
  - module: bar
    symbols:
      - IC.CFFEX
    config:
      freq: 5
    client:
      - bar-py

signal:
  name: bar-py
  module: nickchenyj.bar
  is_python: true
  output:
    module: csv
    config:
      output_dir: output
  config:
    targets:
      - IC.CFFEX

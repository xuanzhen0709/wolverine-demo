env: {}
start: 20250106
end: 20250107
refdata:
  ticker_mapping: HFT

marketdata:
  - module: snapshot
    symbols:
      - ZC.CME
    config:
      dataset: cme
    client:
      - operators

signal:
  name: operators
  module: nickchenyj.operators
  output:
    module: csv
    config:
      output_dir: output/full
  config:
    targets:
      - ZC.CME
      # user defined params can go below

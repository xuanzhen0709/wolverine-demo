env: {}

start: 20230101
end: 20230109
refdata:
  ticker_mapping: HFT

marketdata:
  - module: snapshot
    symbols:
      - IC.CFFEX
      - IF.CFFEX
      - IH.CFFEX
    config:
      dataset: cffex
    client:
      - multiticker-py

signal:
  name: multiticker-py
  module: nickchenyj.multitickers
  is_python: true
  output:
    module: csv
    config:
      output_dir: output
  config:
    targets:
      - IC.CFFEX
    # user defined params can go below
